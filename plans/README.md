# Implementation Plans

Generated by the improve skill on 2026-07-11 (commit `2d919e3`). Execute in the order below. Plans 001–003 are independent and may run in parallel. Plan 005 must complete before Plan 004 (both touch `package.json`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix encryption: plaintext fallback + weak salt + SHA-256 hash | P1 | S | — | DONE |
| 002 | Move Firebase config to environment variables | P1 | S | — | DONE |
| 003 | Fix Firebase subscription leaks | P1 | S | — | DONE |
| 005 | Remove unused dependencies and dead code | P2 | S | — | DONE |
| 004 | Upgrade Next.js to 15.5.x | P2 | M | 005 | DONE |

## Dependency notes

- 004 requires 005 because both modify `package.json`; apply 005 first to avoid merge conflicts.

## Findings considered and rejected

- **Unbounded message pagination (PERF-01)**: not blocking release; Firebase `limitToLast` pagination is the right approach but is a feature addition, not a bug. Deferred to direction backlog.
- **Zero test coverage (TEST-01)**: acknowledged; characterization tests are the prerequisite but represent L effort. Deferred.
- **`minimatch`/`flatted`/`picomatch` ReDoS advisories**: transitive build-time deps via ESLint/Next.js internals, not reachable at runtime in a purely client-side Next.js app.
